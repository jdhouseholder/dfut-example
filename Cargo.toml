[package]
name = "dfut-example"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
rand = "0.8.5"

dfut = { path = "../dfut/dfut" }
dfut-macro = { path = "../dfut/dfut-macro" }
serde = { version = "1.0.197", features = ["derive"] }
